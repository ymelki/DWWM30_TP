{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Nom</th>
                <th>IsVerified</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_user_edit') }}">edit</a>
                </td>
            </tr> 
        </tbody>
    </table>
Voir mes commandes

 <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th> 
            </tr>
        </thead>
        <tbody>
        {% for unefacture in factures %}
            <tr> 
                <td>               
                <a href="{{ path('app_commandes_index', {'id': unefacture.id}) }}">
                {{ unefacture.id }}
                </a></td>
                <td> </td>
            </tr> 
             {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}
