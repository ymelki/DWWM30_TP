<form method="post" action="{{ path('app_admin_aproduit_delete', {'id': aproduit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aproduit.id) }}">
    <button class="btn">Delete</button>
</form>
