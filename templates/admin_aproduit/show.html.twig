{% extends 'base.html.twig' %}

{% block title %}Aproduit{% endblock %}

{% block body %}
    <h1>Aproduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aproduit.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ aproduit.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ aproduit.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_aproduit_index') }}">back to list</a>

    <a href="{{ path('app_admin_aproduit_edit', {'id': aproduit.id}) }}">edit</a>

    {{ include('admin_aproduit/_delete_form.html.twig') }}
{% endblock %}
